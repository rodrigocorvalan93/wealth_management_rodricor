# Template de secrets — copialo a secrets.txt y completá con tus credenciales.
#   cp secrets.example.txt secrets.txt
#
# secrets.txt está en .gitignore — nunca se sube al repo.

# Para byma_loader.py (precios BYMA)
OMS_USER=tu_usuario_oms
OMS_PASS=tu_password_oms

# URL del OMS. Default es latinsecurities. Si tu broker es otro, cambialo:
#   - Cocos:           https://api.cocos.xoms.com.ar/
#   - LatinSecurities: https://api.latinsecurities.matrizoms.com.ar/
BYMA_API_URL=https://api.cocos.xoms.com.ar/

# Para cafci_loader.py (FCIs argentinos)
# Bearer token completo, incluyendo "Bearer "
CAFCI_TOKEN=Bearer eyJ...

# === API backend (api/app.py) ===
# Token de autenticación del API. Generar uno random:
#   python -c "import secrets; print(secrets.token_hex(32))"
# En PythonAnywhere se setea en el WSGI file (no acá).
WM_API_TOKEN=poné_un_token_largo_random_aquí
