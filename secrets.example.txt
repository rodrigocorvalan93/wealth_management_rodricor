# Template de secrets — copialo a secrets.txt y completá con tus credenciales.
#   cp secrets.example.txt secrets.txt
#
# secrets.txt está en .gitignore — nunca se sube al repo.

# Para byma_loader.py (precios BYMA)
OMS_USER=tu_usuario_oms
OMS_PASS=tu_password_oms

# URL del OMS. Default es latinsecurities. Si tu broker es otro, cambialo:
#   - Cocos:           https://api.cocos.xoms.com.ar/
#   - LatinSecurities: https://api.latinsecurities.matrizoms.com.ar/
BYMA_API_URL=https://api.cocos.xoms.com.ar/

# Para cafci_loader.py (FCIs argentinos)
# Bearer token completo, incluyendo "Bearer "
CAFCI_TOKEN=Bearer eyJ...

# === API backend (api/app.py) ===
# Token legacy single-tenant. Solo necesario si NO querés usar email/password
# (api/auth). Para producción usá los signup/login flows.
#   python -c "import secrets; print(secrets.token_hex(32))"
WM_API_TOKEN=poné_un_token_largo_random_aquí

# === Auth con email + password (api/auth.py) ===
# Master key para encriptar credenciales del broker (Fernet, base64).
# Si no se setea, se autogenera en data/.encryption_key (gitignoreado).
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
WM_ENCRYPTION_KEY=

# Email del superadmin: el primer signup con este email se promueve
# automáticamente a is_superadmin=1. Después podés vaciar la var.
WM_BOOTSTRAP_SUPERADMIN_EMAIL=tu@email.com

# Si =1 y SMTP no está configurado, el primer superadmin se crea con
# email_verified=1 automáticamente (modo dev).
WM_AUTO_VERIFY_FIRST_SUPERADMIN=0

# === SMTP — para emails de verify / reset de contraseña ===
# Si falta config, los emails caen al outbox en data/_outbox/ (.eml).
# Gmail con 2FA: app password en https://myaccount.google.com/apppasswords
WM_SMTP_HOST=smtp.gmail.com
WM_SMTP_PORT=587
WM_SMTP_USER=tu_email@gmail.com
WM_SMTP_PASS=tu_app_password
WM_SMTP_FROM=Wealth Management <noreply@tu-dominio.com>
WM_SMTP_TLS=starttls

# URL pública del frontend para los links de email.
WM_APP_URL=https://wm.tu-dominio.com
