# wm_engine — dependencias

# Core
pandas>=2.0
numpy>=1.24
requests>=2.31
openpyxl>=3.1

# Loader cripto
# (urllib3 ya viene con requests)

# Loader US (Yahoo Finance)
yfinance>=0.2.40

# API backend (Fase 2: Flask wrapper para PythonAnywhere)
flask>=3.0

# Encriptación de credenciales per-user (api/credentials.py)
cryptography>=41.0
